export type LoginInputFieldFormKeys = {
  email: string;
  password: string;
};

export type SignupInputFieldFormKeys = {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
};
